---
// Route shell. All page content lives in @components/pages/ClubPage.astro,
// shared by both locales — the two route files differ only by `locale`.
import ClubPage from '@components/pages/ClubPage.astro';
---

<ClubPage locale="fr" />
