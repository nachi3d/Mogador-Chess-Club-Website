---
// Route shell. All page content lives in @components/pages/ProgressPage.astro,
// shared by both locales — the two route files differ only by `locale`.
import ProgressPage from '@components/pages/ProgressPage.astro';
---

<ProgressPage locale="fr" />
