---
// Route shell. All page content lives in @components/pages/MePage.astro,
// shared by both locales — the two route files differ only by `locale`.
import MePage from '@components/pages/MePage.astro';
---

<MePage locale="fr" />
