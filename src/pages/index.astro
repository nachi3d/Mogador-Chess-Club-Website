---
// Route shell. All page content lives in @components/pages/HomePage.astro,
// shared by both locales — the two route files differ only by `locale`.
import HomePage from '@components/pages/HomePage.astro';
---

<HomePage locale="fr" />
