---
// Route shell. All page content lives in @components/pages/SettingsPage.astro,
// shared by both locales — the two route files differ only by `locale`.
import SettingsPage from '@components/pages/SettingsPage.astro';
---

<SettingsPage locale="fr" />
