---
// Route shell. All page content lives in @components/pages/CoursPage.astro,
// shared by both locales — the two route files differ only by `locale`.
import CoursPage from '@components/pages/CoursPage.astro';
---

<CoursPage locale="fr" />
