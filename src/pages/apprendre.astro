---
// Route shell. All page content lives in @components/pages/LearnHubPage.astro,
// shared by both locales — the two route files differ only by `locale`.
import LearnHubPage from '@components/pages/LearnHubPage.astro';
---

<LearnHubPage locale="fr" />
