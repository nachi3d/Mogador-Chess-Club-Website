---
// Route shell. All page content lives in @components/pages/LegalPage.astro,
// shared by both locales — the two route files differ only by `locale`.
import LegalPage from '@components/pages/LegalPage.astro';
---

<LegalPage locale="fr" />
