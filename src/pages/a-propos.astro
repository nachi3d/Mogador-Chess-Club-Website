---
// Route shell. All page content lives in @components/pages/AboutPage.astro,
// shared by both locales — the two route files differ only by `locale`.
import AboutPage from '@components/pages/AboutPage.astro';
---

<AboutPage locale="fr" />
