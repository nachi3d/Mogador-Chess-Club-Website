---
// Route shell. See the FR counterpart.
//
// The SEGMENT is not translated — `/en/progres/`, not `/en/progress/`. One
// segment vocabulary keeps the language switcher a pure prefix swap; the
// visible label is translated, the URL is structural. (CLAUDE.md → Routes.)
import ProgressPage from '@components/pages/ProgressPage.astro';
---

<ProgressPage locale="en" />
