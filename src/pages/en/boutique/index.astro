---
// Route shell. All page content lives in @components/pages/ShopPage.astro,
// shared by both locales — the two route files differ only by `locale`.
import ShopPage from '@components/pages/ShopPage.astro';
---

<ShopPage locale="en" />
