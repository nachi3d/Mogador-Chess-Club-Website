---
// Route shell. See the FR counterpart.
//
// The SEGMENT is not translated — `/en/jouer/`, not `/en/play/`. One segment
// vocabulary keeps the language switcher a pure prefix swap. The visible nav
// label is translated ("Jouer" / "Play"); the URL is structural.
import JouerPage from '@components/pages/JouerPage.astro';
---

<JouerPage locale="en" />
