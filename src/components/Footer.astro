---
/**
 * Site footer — venue, association credit, Nachi3D Labs credit.
 *
 * PORTABILITY: the venue block reads `site.venue` and renders NOTHING when the
 * club has no fixed venue, or when the address is not marked public. No venue
 * string appears anywhere in this file. (CLAUDE.md → Critical Features.)
 */
import type { Locale } from '@config/site';
import { site, hasVenue, venueAddress } from '@config/site';
import { legalPath, privacyPath, settingsPath } from '@i18n/paths';
import { useTranslations } from '@i18n/ui';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = useTranslations(locale);
const address = venueAddress();
const showVenue = hasVenue();
const year = new Date().getFullYear();

/**
 * The source link is a LICENCE OBLIGATION, not a nicety. Chessground is
 * GPL-3.0-or-later, so the site's users must be able to reach its source — and
 * "its users" means every page, not only whoever thinks to open the legal
 * notice. It renders sitewide for that reason; don't demote it to that page.
 * (CLAUDE.md → Licence.)
 */
const sourceUrl = site.legal.source.url;
/** cburnett is CC BY-SA 3.0; this points at the full credit on the legal page. */
const piecesUrl = `${legalPath(locale)}#cburnett`;
---

<footer class="surface-inverse site-footer">
  <div class="container-page footer-inner">
    {
      showVenue && (
        <section class="footer-block">
          <h2>{t('venue.heading')}</h2>
          {/* `venueAddress()` already joins name + address + city + country;
              the map link must not repeat it. */}
          <p>{address ?? `${site.venue.city}, ${site.venue.country}`}</p>
          {site.venue.mapUrl && (
            <p>
              <a href={site.venue.mapUrl} rel="noopener noreferrer" target="_blank">
                {t('venue.map')}
              </a>
            </p>
          )}
        </section>
      )
    }

    <section class="footer-block">
      <h2>{t('footer.association')}</h2>
      <p>
        <a href={site.association.url} rel="noopener noreferrer" target="_blank">
          {site.association.name}
        </a>
      </p>
      <p class="muted">{site.association.handle}</p>
    </section>

    <section class="footer-block">
      <h2>{site.name}</h2>
      <p class="muted">{site.tagline[locale]}</p>
    </section>

    <section class="footer-block">
      <h2>{t('footer.legalHeading')}</h2>
      <ul class="footer-links">
        <li><a href={settingsPath(locale)}>{t('settings.title')}</a></li>
        <li><a href={legalPath(locale)}>{t('footer.legal')}</a></li>
        <li><a href={privacyPath(locale)}>{t('footer.privacy')}</a></li>
        <li>
          <a href={sourceUrl} rel="noopener noreferrer" target="_blank">{t('footer.source')}</a>
        </li>
        <li><a href={piecesUrl}>{t('footer.pieces')}</a></li>
      </ul>
    </section>
  </div>

  <div class="container-page footer-credit">
    <p>
      {site.credit.text[locale]}{' '}
      <a href={site.credit.url} rel="noopener noreferrer" target="_blank">{site.credit.label}</a>
    </p>
    <p class="muted">© {year} {site.name}</p>
  </div>
</footer>

<style>
  .site-footer {
    margin-top: var(--mcc-section-gap);
    padding-block: 2.5rem 1.5rem;
    border-top: 4px solid var(--mcc-accent);
  }

  .footer-inner {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
  }

  .footer-block h2 {
    margin: 0 0 0.5rem;
    color: var(--mcc-accent-on-inverse);
    font-size: 0.8125rem;
    font-family: var(--font-body);
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .footer-block p {
    margin: 0 0 0.35rem;
    font-size: 0.9375rem;
  }

  .footer-links {
    margin: 0;
    padding: 0;
    list-style: none;
    font-size: 0.9375rem;
  }

  .footer-links li {
    margin-bottom: 0.35rem;
  }

  /* green-300 on green-800 = 5.16:1 — verified in scripts/check-contrast.mjs. */
  .muted {
    color: var(--mcc-text-on-inverse-muted);
  }

  .footer-credit {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 0.5rem 1rem;
    margin-top: 2rem;
    padding-top: 1.25rem;
    border-top: 1px solid var(--mcc-border-on-inverse);
    font-size: 0.875rem;
  }

  .footer-credit p {
    margin: 0;
  }
</style>
