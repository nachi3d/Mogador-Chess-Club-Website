# Backlog

Everything decided but not built, in one place. CLAUDE.md points here rather
than keeping a second copy — a list that exists twice is a list that disagrees
with itself.

**Status vocabulary**

| Status | Meaning |
|---|---|
| `soon` | Agreed, no blocker, and wanted next — ahead of anything marked `backlog` |
| `backlog` | Agreed, unscheduled, no blocker |
| `blocked` | Cannot start until something external exists |
| `conditional` | Only happens if a specific judgement goes a particular way |
| `dormant` | Partly built already — schema or field exists, nothing renders it |
| `v3` | Explicitly beyond v2 |
| `seàn` | Waiting on a decision only Seàn can make |

---

## v2 sessions

| Item | Status | Note |
|---|---|---|
| **v2-S2** — Google OAuth, prof-created student accounts, Resend SMTP | `backlog` | ⚠️ **GOOGLE SIGN-IN IS BUILT AND BEHIND `PUBLIC_GOOGLE_AUTH_ENABLED`, DEFAULT OFF** — the code landed before the configuration, because the provider must be switched on in the Supabase dashboard AND an OAuth client must exist in Google Cloud, and nothing in this repository can check either. **Seàn: configure both, complete one real sign-in, then set the build variable.** Prof-created accounts and Resend SMTP are still open. SMTP needs a domain, and `mogadorchess.nachi3dlabs.com` is one — no longer blocked. OAuth and prof-created accounts could ship without it, but all three are one coherent "getting people in" session. `handle_new_user()` already clamps the locale for the Google claim. |
| **⚠️⚠️ GOOGLE IS ALREADY LIVE ON THE PRODUCTION SUPABASE PROJECT — the flag is a UI gate, not a provider gate** | `seàn` | ⚠️ **THIS CONTRADICTS WHAT v0.22.0 WAS PROMOTED BELIEVING**, and the CHANGELOG for that release says "the Google provider is configured on the test project only". **Measured, read-only, on 2026-08-23:** `GET /auth/v1/settings` on production (`vtestpaufxmrvdhgrrsy`) reports `external.google: true`, and `GET /auth/v1/authorize?provider=google` returns **302 to `accounts.google.com`**. So the provider answers on production today. ⚠️ **What this does NOT mean:** it is not a new capability and not a hole — sign-up is already open (`disable_signup: false`, magic link), so anyone who wanted an account could always make one. What it means is that `PUBLIC_GOOGLE_AUTH_ENABLED=false` hides the **button**, not the **endpoint**: somebody who constructs the authorize URL by hand can create a Google-backed account on production before any of the linking questions below are settled. ⚠️ **AND BOTH PROJECTS SHARE ONE GOOGLE `client_id`** (`511211713461-…`), so test and production are not isolated at the Google layer: one OAuth client, one consent screen, one redirect-URI list. ⚠️ **If that client is still in "Testing" mode in Google Cloud, only listed test users can sign in — silently, and on production too.** **Check its publishing status before the flag is flipped**, because "it works for me" is exactly what a Testing-mode client produces for the person who set it up. **Decide:** either disable the provider on production until the feature is wanted, or accept it and say so in the CHANGELOG — the current state is neither. |
| **⚠️ A DIFFERENT Google address silently creates a SECOND account with empty progress** | `seàn` | ⚠️ **THIS IS THE FAILURE A READER ACTUALLY MEETS, AND TODAY NOTHING SAYS A WORD.** Automatic linking keys on the **email address**, so it holds only when the Google address is the same one the magic link used. A parent who signed up as `hakim@example.com` and later presses "Continuer avec Google" while signed into `hakim.essaouira@gmail.com` gets a **new auth user**, a **new child profile**, and a **ledger of zero** — with their real progress intact and invisible on an account they cannot see from where they are standing. ⚠️ **It looks like data loss and it is not**, which is the worst combination: nothing is broken, nothing errors, and the reader has no way to tell what happened. ⚠️ **And it is worse here than on a typical site**, because the progress is a child's — points, rank and attendance somebody earned over weeks (CF33 derives them, so they are not gone, but they hang off the other account). **Options, cheapest first: (a)** say it on `/connexion/` beside the button — one line, "utilisez la même adresse que votre lien e-mail" — which costs nothing and prevents most of it; **(b)** detect it after the fact: on first Google sign-in, if the account has exactly one auto-named child and zero progress, offer "vous aviez déjà un compte ?" with a magic-link path back; **(c)** Supabase's **manual linking** (`linkIdentity()`, needs enabling in the dashboard) so a signed-in reader can attach Google deliberately rather than discovering the fork. ⚠️ **(a) is not optional if the flag is ever flipped** — everything else can wait, but shipping the button with nothing said is shipping the trap. |
| ~~**⚠️ `verify:deploy` can FALSE-FAIL on a fresh deploy's edge cache**~~ | ✅ **DONE (diagnosed, not defeated)** | ⚠️⚠️ **BOTH FIXES THIS ROW PROPOSED WERE TRIED AND MEASURED FAILING, INCLUDING THE ONE IT RECOMMENDED.** The row said "prefer the nonce". A cache-busting query nonce was measured still returning **`CF-Cache-Status: HIT`** on a never-before-seen query string, because Workers static assets normalise the query away — it does not produce a different cache key. And the request `Cache-Control: no-cache` was already being sent; Cloudflare ignores client cache directives by design. **So the cache cannot be busted from the client, and the script now REPORTS the cache status instead of pretending to defeat it.** On a mismatch it prints `cf-cache-status` per document and says: if those read HIT and you deployed minutes ago, suspect the cache before the deploy, probe by hand and re-run. ⚠️ **It still FAILS** — nothing converts a mismatch into a pass, and a retry was deliberately not added, because it would mask a genuinely half-propagated deploy. The point is only that "the live build is NOT this tree" stops being the first and last word when the real story is a thirty-second cache. |
| **⚠️ Passwords were RE-EXAMINED and RE-REJECTED — do not reopen without reading this** | `seàn` | The locked decision is *"Auth is magic-link + Google, **no passwords**"* (CLAUDE.md, `supabase.md` → v2 locked decisions). It was raised again as "identifier + password", for readers with no email address, and rejected again on **three specific grounds** rather than on the fact that it was already decided. ⚠️ **1. SUPABASE HAS NO USERNAME AUTH.** `signInWithPassword` accepts an **email or a phone** and nothing else, so an "identifier" needs a synthetic unroutable address (`someone@…invalid`). Phone+password exists but routes verification and recovery through **SMS**, which the same decision rejects. ⚠️ **2. A SYNTHETIC ADDRESS COLLIDES WITH CRITICAL FEATURE 53.** The auto-created child's placeholder name is derived from the **email local part**, matched exactly — feed it `eleve0042@…invalid` and that string is what lands on the attendance sheet, which is the defect `/bienvenue/` was built to remove. ⚠️⚠️ **3. THE ACCOUNT CANNOT RECOVER ITSELF.** No address means no reset, in a volunteer club with **no support desk**, for users who are **children**. The loss is a ledger somebody earned, the failure is silent, and it arrives weeks later. **What covers the real cases instead:** a child with no email is already covered by the parent/child model; the **autonomous teenager** is an account holding exactly one child and **graduation is one FK update** (CF40/41), so they can live on a parent's account until they have an address; and someone with **neither** email nor a parent account belongs to **prof-created accounts** (same row as Google, v2-S2) — the club is who they would ask anyway. ⚠️ **If it is ever reversed, the only viable non-SMS recovery channel is a STAFF reset**, and that has to be designed with the reversal, not after it. |
| ~~**Apply migrations 0010, 0011 and 0012 to production**~~ | ✅ **DONE** | **Verified against the catalog at the v0.17.0 gate, 2026-08-18**: `profiles.account_shape` 200 (0010), `rebuild_requests` `42501` to `anon` rather than `PGRST205` (0011), `sessions.series_id` 200 (0012). ✅ **The vault entry is live too** — production's `rebuild_requests` carries firings with `dispatched = true`, which is the half no schema query can show. ⚠️ **The `schema_migrations` backfill is still outstanding** and has its own row below. — the v0.14.0/v0.17.0 history follows. ⚠️⚠️ **UPDATED v0.17.0 — THIS IS NOW THREE MIGRATIONS, AND ONE OF THEM IS A SPECIAL CASE.** **0011** (the rebuild trigger) is **already on production, hand-applied**; that is the defect it repairs, and it is idempotent throughout so it is run **over** the hand-applied objects to make the two agree. **0012** adds `sessions.series_id`, which `listSessions()` now names in its select — without it `/admin/seances` renders nothing at all. **0010** is unchanged and still outstanding. **Order: 0010 → 0011 → 0012 → verify against the catalog → register in `schema_migrations` → deploy.** The catalog query and the registration SQL are in [`docs/reference/deployment.md`](./reference/deployment.md). — the original v0.14.0 entry follows. ⚠️⚠️ **THE ONE OUTSTANDING PREREQUISITE FOR v0.14.0, AND IT IS NOT OPTIONAL.** `getProfile()` selects `account_shape`; without 0010 the `PROFILE_COLUMNS` ladder in `supabase.ts` drops production to the pre-0010 rung, so every account reads as **never answered** and `/compte/` shows the neutral copy to families who answered the question. That is a graceful degradation, not a working feature. ⚠️ **`scripts/db-push.mjs` refuses production by design and must keep refusing** — this is a deliberate act against a ref typed by hand. **Order: apply 0010 → verify against the catalog (the queries are in 0010`'`s own footer) → redeploy.** ⚠️ **Also still outstanding: the `schema_migrations` backfill in [`docs/ADMIN.md`](./ADMIN.md)** — production`'`s ledger lists `0001,0002` while the schema demonstrably holds everything through 0009, so a `supabase db push` would try to replay 0003–0009 including 0005`'`s unguarded `drop constraint`. **Do the backfill first.** ✅ **CORRECTED 2026-08-15: production already holds 0008 AND 0009** — verified against the catalog, not the ledger (`account_deletions` answers `42501`, not `PGRST205`). The previous entry claimed both were missing. ✅ **AND ACCOUNTS ARE ALREADY ON IN PRODUCTION** — `/connexion/`, `/compte/` and `/admin/` all answer 200. The dashboard build variable is set; the repo default stays `false` and always will. |
| ~~**Put the Cloudflare deploy hook in PRODUCTION's Vault**~~ | ✅ **DONE** | Confirmed 2026-08-18 by evidence rather than by asking: production's `rebuild_requests` holds firings with `dispatched = true`, so the secret is in the vault and `net.http_post` is reaching Cloudflare. ⚠️ **The TEST project must still never get one** — `recurring-sessions.spec.ts` asserts `dispatched === false` there. — the original entry follows. ⚠️ **THE ONE STEP THAT MAKES 0011 DO ANYTHING, AND IT IS DELIBERATELY NOT IN THIS REPOSITORY.** The hook URL is the credential — anyone holding it spends the account's build minutes — and this repo is public under the GPL. One line in the Supabase SQL editor: `select vault.create_secret('https://…', 'cloudflare_deploy_hook', 'Cloudflare deploy hook');`. Until it exists the trigger **logs every firing and sends nothing**, which is a working state, not a broken one — it is exactly what every test project must stay in. ⚠️ **NEVER put one on the TEST project**: every e2e run would start spending real build minutes, and `recurring-sessions.spec.ts` asserts `dispatched === false` for that reason. Snippets (create, rotate, confirm-without-printing) in [`docs/reference/deployment.md`](./reference/deployment.md). |
| **WebKit gives no date/time picker on `/admin/seances`** | `backlog` | ⚠️ **MEASURED, NOT ASSUMED, at the v0.17.0 gate**: Playwright's WebKit reports both `#session-when` (`datetime-local`) and `#session-until` (`date`) as `type: "text"` — the inputs degrade to bare text boxes. Nothing breaks (values round-trip and `expandSeries` parses them; the preview rendered all 13 dates correctly), but a prof on Safari or an iPhone types `2029-03-07T18:00` by hand where a Chrome user gets a calendar — on the one form the club uses weekly, on the device they are most likely to use in the room. ⚠️ **Do NOT 'fix' this with a date-picker dependency without checking the licence** (GPL repo) and Critical Feature 9. A `pattern` + `inputmode` + a worked example in the hint is probably the cheap 80%. |
| **A staff-visible “when did the site last rebuild” line** | `backlog` | `public.rebuild_requests` records every firing and staff hold `select` on it, but nothing renders it. It would sit naturally beside the staleness banner on `/admin/seances` — “dernière reconstruction demandée à 14:02” closes the loop between pressing Publier and the public page moving. Not built in v0.17.0 because the banner already answers the question a prof actually asks (“is the site up to date?”) and this only answers “did we ask?”. |
| ~~**⚠️ `test:branch` DOES NOT PRESERVE ITS ARTEFACTS**~~ | ✅ **DONE** | Copies into `gate-logs/branch-<stamp>/` and **names the path before the advice** on a red run. ⚠️ **It needed this more than the gate did**, which is not where the fix landed first: the branch runner runs one project, so nothing clears `test-results/` mid-run — which made it look safe. The artefacts survive only until the NEXT run, and the next run is the most natural response to a red branch gate. ⚠️ **Watched to work, and it earned itself immediately**: forcing a failure preserved **four** artefact directories, three of which were a real `exercise-filters` regression from the same session's content cuts rather than the deliberate probe — a breakage that would otherwise have been found later, or blamed on something else. |
| ~~**⚠️ EXERCISE DUPLICATES — the audit**~~ | ✅ **DONE** | Seàn cut three from the audit: `mat-du-couloir-dame`, `decouverte-qui-gagne-la-dame` and `mat-etouffe-cavalier`. **24 remain.** Kept deliberately: both surviving couloir siblings, all the forks and both deflection-mates — same tactic, different geometry, and drilling a motif twice is the point. ⚠️⚠️ **The cut moved the points ceiling and the rank thresholds had to move with it** (965 → 900), which pushed Dame above the LEARNING ceiling and quietly made the top rank unreachable without games — the shrink direction nobody plans for. See the note in `points.ts`. ⚠️ **It also broke a spec that was working perfectly**: `exercise-filters.spec.ts` pinned a `>= 27` floor written as a one-way ratchet against growth. |
| ~~**⚠️ A MECHANIC THAT BRINGS STUDENTS BACK**~~ | ✅ **DONE** | Seàn chose the **weekly habit mark**; the win streak was explicitly rejected, because a mechanic that breaks on a loss is what Critical Feature 35 exists to prevent. Built on `/progres/`: it counts weeks that HAPPENED, never a consecutive-week counter — that would reintroduce Critical Feature 34's daily-streak punishment one rhythm up. ⚠️ **Derived from timestamps that already exist** (`solvedAt` and the game log), so nothing is stored and nothing can disagree with the records. ⚠️ **ISO weeks**, so a Saturday session and the Sunday after it are ONE mark — the club meets at the weekend, and a Sunday-start week would split one visit into two. ⚠️ **The unmarked week names an ACTION rather than an absence**, and a spec asserts the line never says *perdu / série / raté / streak / missed* and that a marked and an unmarked week are the **same colour**. **Option C ("revenez pour…", a named next thing) remains unbuilt and is still the safest next one.** |
| **Monthly recurrence (“the third Wednesday”)** | `backlog` | Deliberately not built. Weekly and fortnightly are what the club runs; monthly is where a date list stops being obvious and a rule starts being tempting. ⚠️ **If it is ever wanted, it is a new entry in `STEP_DAYS` and a label — NOT an RRULE engine and NOT a recurrence table** (Critical Feature 69). |
| **Containment: `/parametres/`, `/connexion/`, `/bienvenue/` still uncontained** | `claude` | The containment work took the brief's priority order and stopped where it stopped. **Done:** `/admin/seances/` (three cards, four field groups, the form behind a disclosure), `/compte/` (three cards of one facture, one rhythm), `/agenda/` (session cards, and the coloured left edge removed from every one of them), `/admin/`, `/admin/comptes/`, `/admin/eleve/` (eight `.admin-block`s became cards) and `/admin/eleves/` (the class table in a card with a section header). **Not done:** `/parametres/`, `/connexion/` and `/bienvenue/`. ⚠️ **None of them is broken** — `/connexion/` already uses `.auth-card`, which `cards.css` contains properly — but they have no section headers and their gaps still come from each component's own margins rather than the scale. The vocabulary they need already exists (`.mcc-card`, `.mcc-section-head`, `.mcc-field`, `--mcc-space-*`), so this is application rather than design. |
| **Touch targets in the shared header and footer are below 48px** | `seàn` | Measured at 360px on `/admin/seances/` after the containment work: **every control in the admin CONTENT clears 48px**, and what remains under it is site-wide chrome — `a.brand` 28px, `a.account-link` 34px, `button.theme-toggle` 44px, and the footer's inline text links at 17–19px. ⚠️ **The footer links are inline text and WCAG 2.5.8 exempts them**; the header three are real targets. Raising them is a **site-wide header decision**, not an admin one, and it changes every page — which is why it was left rather than folded into a structure-only brief. **The 44px floor site-wide is deliberate and documented** (`controls.css`, measured by `motion.spec.ts`); this row is only about the header's own three controls. |
| **⚠️ CLAUDE.md — the STRUCTURAL question, still Seàn's** | `seàn` | **Split again at v0.20.0: 124,481 → 119,510 (83% → 80%).** Three blocks moved verbatim into existing reference files — the gate audit and the three-gate hydration diagnosis to [`testing.md`](./reference/testing.md), the dated schema-status paragraph and the two accounts-OFF build-leak incidents to [`deployment.md`](./reference/deployment.md). `check-split.mjs` clean: 1,345 lines stayed, 136 moved, **nothing declared obsolete** (recorded as such in `.split-obsolete.txt`, because "no entries" and "nobody checked" look identical from the outside). ⚠️ **THAT IS A REPRIEVE, NOT A FIX — roughly two or three sessions before it warns again.** ⚠️ **AND THE QUESTION IS UNCHANGED FROM v0.18.0 AND v0.19.0, WHICH IS ITSELF THE FINDING: three splits have now moved everything narrative that had a home, and the file is index-shaped.** The two largest blocks are **Critical Features (14,877)** and the **routes table (4,860)**, and both are things a session breaks *without going looking*, which is the test for staying. **A further reduction would be a TRIM, which the rule forbids.** So: does the Critical Features list become a linked file carrying only its headlines inline, accepting that a rule one click away is a rule some sessions will not read? ⚠️ **Do not answer it by shaving prose**, and do not answer it in a feature branch — it is the one change that alters how every future session reads the rules. |
| **⚠️ `verify:deploy` has been BLIND for two releases running — make the discriminator a repo script** | `claude` | **MEASURED, TWICE.** Its three compared documents — `/`, `/exercices/mat-du-couloir/`, `/progres/` — were **byte-identical between the old and new tree at BOTH the v0.18.0 and the v0.19.0 gate**, once `/_astro` fingerprints are normalised away. v0.18.0 was session booking (admin + agenda) and v0.19.0 was a site-wide CSS/markup change that still did not move those three documents. ⚠️ **So the tool printed, or would have printed, a green tick either way, and a content DISCRIMINATOR carried both releases.** `docs/reference/deployment.md` already records the residual gap and the manual recipe — pick a marker from a document the release DID change, prove it absent from the old tree, carry a negative control — and that recipe has now been hand-written twice in a scratchpad and thrown away twice. ⚠️ **THE RECIPE IS THE PART THAT MUST NOT BE RE-DERIVED UNDER TIME PRESSURE AT A PROMOTION**, which is exactly when it is used. **Proposal:** `scripts/verify-content.mjs`, taking markers from a small committed list, asserting (1) marker ABSENT from a named git ref's build or from the currently-live site captured before the push, (2) marker PRESENT live after, (3) a NEGATIVE control that never existed, (4) a POSITIVE control present in both — the four checks both hand-written versions converged on. ⚠️ **It does not replace `verify:deploy`** — that proves the whole rendered surface matches, which a marker cannot; run both, and on a release that misses the three documents the discriminator is the one carrying the weight. |
| **⚠️ CI GATE — BUILT, AND WAITING ON FIVE REPOSITORY SECRETS** | `seàn` | ⚠️ **THE WORKFLOW IS IN THE REPO AND WILL FAIL ITS FIRST RUN UNTIL THE SECRETS EXIST — that is the intended first signal, not a defect.** `.github/workflows/gate.yml` runs the five projects in parallel plus the accounts-OFF sliver on `ubuntu-latest`, where Smart App Control does not exist. **Seàn: set these five repository secrets** (Settings → Secrets and variables → Actions), all from the **TEST** project, never production: `TEST_PUBLIC_SUPABASE_URL`, `TEST_PUBLIC_SUPABASE_ANON_KEY`, `TEST_SUPABASE_SERVICE_ROLE`, `TEST_SUPABASE_PASSWORD`, `SUPABASE_PRODUCTION_REF`. ⚠️ **`SUPABASE_PRODUCTION_REF` IS THE INTERLOCK'S WHOLE JUDGEMENT** — it is the one value that names production, it is deliberately unprefixed, and without it every run aborts. ⚠️ **The workflow WRITES `.env.test` from the secrets rather than exporting them**, because `tests/e2e/env.ts` reads only that file and CLAUDE.md forbids widening it to `process.env`. ⚠️ **`scripts/ci-preflight.mjs` runs in EVERY job**, not just once: a missing secret would otherwise skip every auth spec and still go green, which is the same class of failure as a zero-test sliver. **Verified locally on the same code path**: real credentials pass; production ref set equal to the test ref makes both the preflight AND Playwright's config load abort with `"…IS the production ref. Refusing."`; an absent file fails the preflight naming all five secrets. ⚠️ **NOT YET MEASURED ON CI** — there is no `gh` CLI on this machine, so the run time, and the first green run, are Seàn's to observe. Estimate ~10-15 min wall-clock, five jobs in parallel. ⚠️ **CORRECTED**: the CI build does NOT bake the fallback — `playwright.config.ts` passes the TEST project URL and key into the build, so `fetch-agenda` reads the real table (measured: 3 sessions, newest 2027-01-08). The expiry trap applied only to the sliver job, which had no `.env.test`; it now has one, which also stops Critical Feature 18 passing trivially. |
| ~~**`test:branch` still runs the accounts-OFF shape**~~ | ✅ **DONE** | Closed in v0.18.0, raised because it is a hole in the **daily** loop rather than the release loop. `NEEDS_ACCOUNTS_ON` now lives in [`scripts/lanes.mjs`](../scripts/lanes.mjs) — one list, read by `test-branch.mjs` and by the audit — and the branch runner builds accounts-ON when the selection contains any of those twelve specs, OFF otherwise. ⚠️ **The inline copy it replaced was missing `booking`, `booking-ui` and `recurring-sessions`.** **Cost:** nothing at all for a branch that touches no account code; for one that does, a slightly longer build and some of Supabase's per-IP verify quota — which `--workers=2` already exists to survive. An explicit `PUBLIC_AUTH_ENABLED` still wins, and a selection wanting both shapes says so rather than being silently half-covered. |
| ~~**⚠️⚠️ `verify:deploy` CANNOT PASS ON `/` FROM A MACHINE WITHOUT AGENDA CREDENTIALS**~~ | ✅ **DONE** | Fixed on `claude/agenda-fallback-guard`. The next-session **value** is normalised out of `/`'s comparison, the same trick already used for `/_astro` fingerprints; only the value goes, never the structure, so a release that changes how the block is BUILT is still caught. ⚠️ **`/` was kept** — swapping it for a quieter document was the obvious fix and the wrong one, since `/` is the page most releases touch. ⚠️ **The normalisation counts its own matches on both sides and warns if it ever gets zero**, so it cannot silently become a no-op — verified by renaming the class and watching the warning fire. Measured against the live v0.20.0 build: `/` went from "the live build is NOT this tree" to **66,358 bytes match**. |
| ~~**⚠️ `agenda.fallback.json` HAS DRIFTED, AND A STALE FALLBACK IS WORSE THAN AN EMPTY ONE**~~ | ✅ **DONE** | Both halves closed on `claude/agenda-fallback-guard`. **The guard:** `fetch-agenda.mjs` now **fails the build** when the committed fallback is used and every session in it has passed — the same reasoning that already makes an empty agenda fatal, since `smoke:prod` is structurally blind to a stale one (it counts rows, sees one, passes). It **warns for fourteen days first**, so the failure is never the first anyone hears of it; both paths were watched to fire. **The refresh:** 1 session → 3. ⚠️ **The fallback was not WRONG, it was INCOMPLETE** — its 12 September session is real, but the two nearer ones were missing, so the dashboard named 12 September when the true next session is 29 August. ⚠️ **Rebuilt from the DEPLOYED agenda, not the table** (no credentials on this machine), so it cannot see a `draft` session and is current only as of the v0.20.0 build; `titleFr` and `overbookMargin` are inferences from the schema default and from the one record whose true values were already committed. **Re-run it from the real table whenever credentials are to hand.** |
| ~~**⚠️ The gate DESTROYS the failure artefacts it tells you to read**~~ | ✅ **DONE** | `test-release.mjs` copies each project's artefacts out of `test-results/` immediately after that project runs, into `gate-logs/artefacts-<shape>-<stamp>/<project>/`, before the next run clears the directory. ⚠️ **`preserveOutput` alone was NOT the fix** — it governs output for PASSING tests and does not stop the next run clearing the directory, and six runs share one. The artefacts have to LEAVE `test-results/` between runs. ⚠️ **The sweep was CHECKED rather than assumed**, as this row warned: `demo.mjs --sweep-only` kills processes and touches no files, so a copy is safe from it. ⚠️ **Watched to work against a deliberate failure** — an `error-context.md` and its screenshot preserved and read back — and the green path confirmed to report "no failure artefacts". ⚠️⚠️ **AND THE FIRST VERSION PRINTED THE POINTER ONLY ON THE GREEN PATH**, which is exactly backwards: the gate exits before it when something fails, so the path was missing precisely when somebody needed it. Found by running the real script against a failure rather than by reading it. It never fails the gate: a copy that throws is reported and the run continues. |
| **`touch-focus.spec.ts:230` fails on webkit under fan-out but passes serially** | `backlog` | ⚠️ **THE `play.spec.ts` HALF OF THIS ROW IS CLOSED, AND IT WAS NOT FOCUS MODALITY.** This row spent three gates arguing that because `touch-focus.spec.ts:230` and `play.spec.ts:111`/`:153` are all FOCUS-MODALITY assertions, `useMoveSource.ts` was the common suspect. ⚠️ **THAT INFERENCE WAS WRONG, AND IT WAS WRONG IN AN INSTRUCTIVE WAY: THE TEST NAMES WERE THE ONLY THING THE FAILURES HAD IN COMMON.** Two of the three never reached an assertion of their own — they died inside `startGame()`, at the shared wait for `data-phase="playing"`. Read by their stacks rather than their titles they had nothing in common but a helper. The cause was **a server-rendered control that is live-looking and inert until its island hydrates**: `/jouer/`'s start button had no handler behind it, and a press in that window was swallowed in silence. ⚠️ **THE DISCRIMINATOR WAS IN THE ARTEFACT AT ALL THREE GATES** — `error-context.md` showed `data-phase="setup"` with the error alert **EMPTY**, which the load-failure path cannot produce. Fixed in `821ef9a` (`PlayView` publishes `data-ready`; the button and both fieldsets are disabled until it is true), reproduced deliberately by delaying the island chunk 4s via `page.route` (1-in-60 naturally → 100%), and verified at **176 consecutive clean executions**. ⚠️ **`useMoveSource.ts` WAS AUDITED AND IS CLEAR — do not re-open it on this evidence.** ⚠️ **WHAT REMAINS OPEN IS THE WEBKIT HALF, AND THE FIX ABOVE DOES NOT EXPLAIN IT.** `touch-focus.spec.ts:230` drives the **exercise** island on `/exercices/opposition-et-mat/`, not `/jouer/`, and its `openExercise` already waited on `data-ready`. The v0.20.0 audit found and fixed one live-looking control there (the **hint** button — see the CHANGELOG), but `:230` touches neither it nor the move field, both of which shipped correctly disabled. So the known facts are unchanged: it failed the v0.17.0 accounts-OFF matrix twice with an assertion naming values — `Expected: false / Received: true`, *"the field was re-focused after a TAPPED move, because an earlier move had been typed"* — flaked once in accounts-ON, and passed **7/7** on a serial webkit re-run. ⚠️ **MEMORY IS RULED OUT** (webkit troughs 6.21 GB / 5.76 GB, far above the ~2 GB starvation line), so it is load-sensitive without being starved. ⚠️ **AND THE `play.spec.ts` LESSON APPLIES HERE TOO: PASSING SERIALLY IS NOT A CLEAN BILL.** **Next step:** run `touch-focus.spec.ts` on webkit at `--workers=3` in isolation on a quiet machine, and **read `error-context.md` before the re-run** — that is what settled the other half after three gates of guessing. ⚠️ **Smart App Control currently blocks WebKit on this machine** (see the row below), so this is blocked on that, not on analysis. |
| **`attendance-timing.spec.ts` can lose a mark under fan-out** | `backlog` | Observed once during the v0.17.0 gate: `19 sur 28 marqués` after twenty successful taps, on the parallel chromium run. **A serial re-run of the same set passed 192/192**, which is the documented arbiter, so it is not a regression — but it is not nothing either. The likely mechanism is the register's SECOND `loadRegister()` (the reader picks a session while the preselect is still resolving) legitimately repainting from a read that predates some taps; the generation counter drops a stale answer but cannot drop a legitimately newer one whose data is older. ⚠️ **The marks were all durable in Postgres — the defect is the prof's COUNT, which is the thing that stops them losing their place in a room of twenty children.** A real fix probably means merging the read with what is already on screen rather than clearing `marks`. |
| **⚠️ v0.13.0 WAS NEVER DEPLOYED — verify the next one lands** | `seàn` | The onboarding screen shipped to `main` and tagged as v0.13.0 on 2026-08-14 at 20:55 UTC, and the site never served it: `/bienvenue/` and `/admin/comptes/` both **404 on production**, and the deployed callback chunk hard-codes `window.location.replace('/compte/')` with no `onboarded_at` read in it. The last deployment is **2026-08-14T22:02:45Z — 67 minutes AFTER the merge**, and it replaced the Workers Build that the `main` push triggered at 20:57:28Z. That is the documented two-deploy-paths hazard (CLAUDE.md → Deployment): **last writer wins, and the later writer was building an older tree.** ⚠️ **Verify by OUTPUT after the next promotion**, per the same rule — fetch `/bienvenue/` and expect 200, and grep the callback chunk for `bienvenue`. `Source: Unknown (deployment)` in `wrangler deployments list` does not tell the paths apart. |
| **Modèle parent + profils enfants** | ✅ **BUILT** | Migration 0005 shipped: `child_profiles` with its own PK and a nullable `account_id`; progress, games, attendance and awards repointed to the child; `graduate_child()` proved on the test project (one FK update, progress followed, zero rows copied); live RLS audit clean, including "a parent cannot take over another family's child". Client side: `src/lib/child.ts` resolves the active child as CONTEXT — `progress.ts` kept its API shape — and the family section is on `/compte/`, with the "Qui joue ?" picker inside it. ⚠️ **A parent can now add, rename and remove their own children.** For two releases they could not: the section hid itself at one child or fewer and a new account is given exactly one, so the add form was unreachable for every real account while RLS permitted the insert and every spec stayed green. Fixed in `FamilySection.astro`, Critical Feature 48, `tests/e2e/family.spec.ts`. See CLAUDE.md → "The parent/child model". **What remains is the surfaces built on it**, below. |
| ~~**v2-S3** — progress sync + `localStorage` import~~ | **done** | Migration 0003 (`kind` discriminator + `game_results`), RLS audited live against the running database, `progress-sync.ts` behind `progress.ts`, an idempotent merge, a bounded offline queue, and the `/compte` + `/progres` surfaces. ⚠️ `PUBLIC_AUTH_ENABLED` is still OFF and is now the only thing between this and accounts being live. |
| **v2-S4 (part 1)** — the role boundary | **done** | Migration 0004 (`point_awards`), a 22-assertion live RLS/GRANT audit, and `role-separation.spec.ts`. The boundary is proven; the surfaces are not built. |
| ~~**v2-S4 (part 2)** — the admin surfaces~~ | **done** | `/admin` (dashboard), `/admin/eleves` (the class — **children, not accounts**), `/admin/eleve/?id=` (one learner: the student’s own progress, plus attendance and awards) and `/admin/seances` (session CRUD + the register). **No new migration was needed** — 0001/0004/0005 already carried every table and policy, which is what "the boundary underneath is already proven" meant. FR only, and now Critical Feature 43 so it stays that way. The register is one tap per child, optimistic, no modal and no save button; **measured at 59 ms of UI per child, 1.18 s for twenty taps, durable in 1.47 s** (`attendance-timing.spec.ts`). `role-separation.spec.ts` grew from 8 to 15 assertions, all through PostgREST with real tokens. ⚠️ **Creating a student from the admin UI is deliberately NOT built** — staff hold SELECT on `child_profiles` and nothing else; see "Guest attendance", which is the feature that needs it. |
| ~~**v2-S4 (part 3)** — the agenda moves to the database~~ | **done** | Build-time read, as decided: `scripts/fetch-agenda.mjs` → `src/data/agenda.json` → `src/lib/agenda.ts`. The git collection is retired and `content.config.ts` says why. Migration 0006 widened the public select policy so a **cancelled** session stays visible with its state (CF46 was only half kept), and migrated the single git entry with a fixed uuid. ⚠️ **The stated cost is real and is now made LOUD rather than solved**: `/admin/seances` knows what the deployed build baked, compares it to the live table by fingerprint, and tells the prof when the public agenda has not caught up. ⚠️ **Two dashboard steps remain and they are Seàn's** — `PUBLIC_SUPABASE_*` as Cloudflare BUILD variables (without them no prof can change the agenda at all), and a nightly rebuild as the self-healing floor. ✅ **The third is BUILT (v0.17.0)**: migration 0011 pokes the deploy hook from a statement-level trigger, which is what the Supabase Webhooks UI could not do on this project. Both in `docs/reference/deployment.md`. |
| **Raise the TEST project`'`s auth rate limit in the Supabase dashboard** | `seàn` | ⚠️ **THE SUITE IS AT THE CEILING, AND THAT IS THE ACTUAL FIX.** Every signed-in spec mints an account and verifies a magic link, and `/auth/v1/verify` is rate limited per IP. Measured against the test project: **~22 verifications in 7 seconds returns `429 over_request_rate_limit`** from cold, with no `Retry-After` — that is the ONSET, **not the window**, and under real suite load a 40s backoff exhausts still limited. The CI gate runs **~257 verifications** (chromium 168 auth tests + webkit 89), which crossed it at gate run #5. Mitigated in the repo (chromium and webkit now share ONE job so the two auth-carrying lanes never overlap; `test-branch.mjs` caps auth-heavy selections at `--workers=2`; `followMagicLink()` and `anonClientAsUser()` back off 10s/30s on a positively identified 429 and then name it), **but mitigation is not headroom — serialising only moved the peak below the line, it did not raise the line.** ⚠️ **EXACTLY WHERE:** Supabase dashboard → project **`puhhrqbgcobblowengii`** → **Authentication** → **Rate Limits** → **"Rate limit for token verifications"** (the one governing `/auth/v1/verify`; the email/SMS/sign-in rows are different limits and are not the problem). Default is **30 per 5 minutes**; serialised, chromium alone needs ~65 in its busiest 5 minutes, so set **150**, or **300** if a developer should be able to run the suite locally while CI is running. ⚠️⚠️ **CHECK THE REF BEFORE TOUCHING ANYTHING — PRODUCTION IS `vtestpaufxmrvdhgrrsy`, WHICH BEGINS WITH "vtest" AND READS LIKE THE TEST PROJECT.** Read the ref, never the vibe of the ref. **Never raise it on production** — there the limit is doing its job. |
| ~~**Split CLAUDE.md — the size guard is warning**~~ | **done** | Split at v0.18.0: **120,306 → 111,582 characters (80% → 74%)**, as the release's own first act rather than after the feature work. **Seventeen blocks moved VERBATIM** under `Read when` lines — the account and admin surfaces, the baked agenda, session booking, the migration checklist, self-deletion, the test interlock and `demo:accounts` to [`docs/reference/supabase.md`](./reference/supabase.md); the anon-key schema probe to [`docs/reference/deployment.md`](./reference/deployment.md); the environment tells, the four board gates, the two-shape gate, the serial matrix and the removed critical-path trigger to [`docs/reference/testing.md`](./reference/testing.md); the facade and fixture rules to [`docs/reference/video.md`](./reference/video.md); the 768px rules to [`docs/reference/ui-navigation.md`](./reference/ui-navigation.md). `check-split.mjs`: **344 moved, 1,090 stayed, nothing lost, nothing newly declared obsolete.** ⚠️ **BUT THE HEADROOM IS STILL ONLY ~8,400 CHARACTERS, AND THE NEXT ONE CANNOT BE THE SAME MOVE.** The narrative is gone; what is left is index-shaped — **Critical Features (14,377)** and the **routes table (4,945)** are the two largest blocks and both are exactly the case the rule says stays, because a session breaks them *without going looking*. A further reduction would be a **trim**, which the rule forbids. **When it warns again the question is structural and it is Seàn's:** does the Critical Features list become a linked file with only its headlines inline — accepting that a rule one click away is a rule some sessions will not read — or does the hard limit move? Do not answer it by shaving prose. |
| **Trap "read" tracking** | `backlog` | The Apprendre chooser wants "X sur Y pièges lus" and cannot have it: nothing records that a trap has been READ. `progress.ts` tracks SOLVING — a boolean earned by doing something judged — and reading is not that. Adding it means a new semantic in the one module that is the single migration point for local progress, plus a decision about what counts as read (opened? scrolled? stepped to the last ply?). The card shows how many traps exist instead. ⚠️ **Do not add a key to `mcc:progress:v1` for this without deciding the semantic first** — a `read` flag that means "the page loaded" would inflate every count on the site and could not be walked back. |
| **`/apprendre-les-bases/` vs `/apprendre/`** | `note` | Two routes whose prefixes differ only by a trailing slash, and the bar matches on `startsWith`. It is correct today and asserted by `mobile-app.spec.ts` at both, but it is one careless `startsWith('/apprendre')` away from the tutorial being swallowed by the hub. If a third `/apprendre*` route ever lands, rename rather than adding a fourth prefix to reason about. |
| **Guest attendance** | `backlog` | Attendance assumes a student with an account, so a teenager who has never signed in cannot be marked. BabyClub solved the same shape with **guest bookings** — a row keyed by a name rather than a profile — and that is the reference to look at when this is wanted. Not built here: it means attendance rows without a `profile_id`, which changes the FK and the RLS on a table that currently has a clean owner rule. |
| ~~**Session booking**~~ | **done** | Migration 0013: `sessions.capacity` + `overbook_margin`, a `bookings` table with a **partial unique index** so a cancellation frees the place, and `create_booking()` / `cancel_booking()` / `session_availability()`. ⚠️ **Capacity is enforced in Postgres, not in client code** — there is no insert policy for a parent at all, and the function locks the session row before it counts. **Measured, not assumed: six concurrent bookings for three places leave exactly three**, asked of the database afterwards. 28-assertion live RLS/GRANT audit clean. ⚠️ **A booking fires NO rebuild** and a spec counts `rebuild_requests` to keep that true. Member surface on `/agenda/` (FR/EN), prof surface on `/admin/seances` (capacity + margin fields, the attendee list with parent phone, booked children leading the register). |
| **⚠️ Apply migration 0013 to production** | `seàn` | ⚠️ **NOT APPLIED — and the ORDER matters more than usual here.** `fetch-agenda.mjs` now names `capacity` and `overbook_margin` in its select. A **column ladder** was added for exactly this reason, so a credentialed build against a pre-0013 production database degrades to the old columns and bakes an agenda without capacity rather than failing — but that is a safety net, not the plan. **Migrations FIRST, then deploy**, per the agenda incident. Verify against the CATALOG, not `schema_migrations`: `select to_regclass('public.bookings') is not null, to_regprocedure('public.create_booking(uuid,uuid)') is not null, exists (select 1 from information_schema.columns where table_name='sessions' and column_name='capacity');` — expect `t, t, t`. |
| **A session detail route** | `backlog` | The booking controls live on the `/agenda/` card because a session has no page of its own. That is fine at three sessions and starts to strain at thirty — a card carrying places, a book button per child and a note is already the densest thing on the page. ⚠️ **If one is ever added it is a ROUTE PER SESSION on a static build**, so it must come from the baked snapshot like the agenda does, and `isRoutable()`/`isListed()` thinking applies. |
| **A waiting list** | `backlog` | Deliberately not built: full means the button is disabled and says «Complet». A waiting list is a promise to contact somebody, which is an outbound channel this club does not have and a moderation obligation it cannot staff (the in-app communication rule). ⚠️ **If it is ever wanted, the honest version is "tell the prof", not "notify the parent".** |
| **v2-S5** — progress charts | `backlog` | Needs S3's data before it can show anything true. |
| **Student groups** (all profs currently see all students) | `backlog` | Deferred to v2.1 by decision, not oversight. |

### Modèle parent + profils enfants — ✅ DECIDED, and now BUILT (migration 0005)

✅ **Seàn's decision: parent-held accounts with child profiles beneath is THE
model.** Not one option among several, and not an open question — **most
students at Dar Souiri arrive with a parent**, so the family is the ordinary
case and the schema should say so.

**A parent holds the account.** `auth.users` is the parent: they sign in, they
receive the email, they enrol children in sessions. Each **child is a profile
beneath it**, with no credentials of its own, carrying progress, points, rank
and attendance. The same shape as BabyClub's `profiles → children`.

⚠️ **A DIRECT STUDENT ACCOUNT REMAINS THE SECONDARY CASE, not a deleted one.**
An autonomous teenager who signs up alone must still work — the model is
"parent-first", not "parent-only". Both shapes exist in the same schema.

⚠️ **AND IT STILL CANNOT WAIT, for a different reason now.** Today
`profiles.id` **is** `auth.users.id` — identity and person are one row, so a
child with no login has nowhere to live. Every account opened before the schema
changes is keyed to a login with real progress hanging off it. The decision is
made; what is left is doing it **before** accounts go live, or paying for it as
a data rescue afterwards.

- **Profile picker on app open — "Qui joue ?", Netflix-style.** A choice, not a
  password: a twelve-year-old should not be managing credentials, and the parent
  keeps oversight without standing over them. **Remembered per device**, so a
  child's own phone asks once and then stops asking.
- **One purse per child.** Individual merit stays visible — a child can see what
  *they* earned, which is the whole point of the ledger. ⚠️ **Do NOT invent a
  shared family wallet**: it blurs who earned what, which is exactly what E3's
  derived-not-banked rule exists to keep legible. A purchase may draw from
  **several purses**, with the split recorded on the order.
- **Attendance and exercises attach to the CHILD profile, not the parent.** A
  prof marks children, not accounts.
- ⚠️ **GRADUATION IS A REQUIREMENT, NOT A QUESTION.** A sixteen-year-old will
  not want to go through their mother's account. A child profile must be able to
  graduate into its own account **carrying its progress with it**, and the
  schema must make that **a migration of ONE FK, not a data rescue** — which in
  practice means a child profile needs its own primary key and a *nullable* link
  to the account that currently holds it, rather than being keyed on
  `auth.users.id` as profiles are today. If graduating a profile ever requires
  copying rows between tables, the shape is wrong.

#### ⚠️ WHAT THIS NOW BINDS — design against it, do not retrofit

The decision is taken, so these are no longer "interactions to consider". They
are constraints on work that has not been built yet, and each one is cheap to
honour now and expensive to retrofit:

| | |
|---|---|
| **v2-S4 part 2** (the admin surfaces) | ⚠️ **Must be designed against this model.** Attendance attaches to the CHILD profile; `/admin/eleves` lists children, not accounts; a parent with three children is three rows in the class table and one row in `auth.users`. Building it against "one account = one student" and fixing it later means rewriting the marker, the class table and the FK together. |
| **E8 shop** | One purse per child. A purchase may draw from **several purses**, with the split recorded on the order. ⚠️ Never a shared family wallet — it blurs who earned what, which is the same legibility E3's derived-not-banked rule protects. |
| **Points and rank** | One purse per child, so individual merit stays visible. A child sees what *they* earned. |
| The minors privacy paragraph | Already models a **guardian** email, so the policy is closer to this model than the schema is. |

**No longer blocked.** What remains is sequencing: the schema change should land
before accounts are turned on for families, because a club where parents enrol
is a different product from a club of teenagers with their own email addresses,
and the schema cannot straddle both after the fact.

## Deployment and domain

| Item | Status | Note |
|---|---|---|
| **Attach the custom domain in Cloudflare** | `seàn` | ⚠️ **The one manual step, and the only thing left.** Workers & Pages → `mogador-chess-club-website` → Settings → Domains & Routes → Add → **Custom domain** → `mogadorchess.nachi3dlabs.com`. Cloudflare creates the DNS record and issues the certificate itself — **do not add a CNAME by hand**, that is the usual way to get this wrong. `npx wrangler deploy` also provisions it from `wrangler.jsonc`, so this is belt-and-braces for the first deploy. Verify with `npm run smoke:prod`, which fails loudly and specifically until the certificate is active. |
| ~~Publish on `mogadorchess.nachi3dlabs.com`~~ | **done** | All four touch points landed: `site.url`, `astro.config.mjs` `site`, `routes[0]` with `custom_domain: true` in `wrangler.jsonc` (**still no `main`**), and `scripts/smoke-prod.mjs` + `npm run smoke:prod`, which did not exist before. See CLAUDE.md → Deployment. |
| ~~**⚠️ The live `/agenda/` is BLANK and only a build will fix it**~~ | **done** | ⚠️ **The public agenda was blank for roughly fourteen hours on 2026-08-14** — "Aucune séance programmée pour le moment" in both locales. Nothing in the repo was wrong: the deployed build simply **predated the data**. Serving version `45e06d08` was created `2026-08-13T23:41:11Z`; the `sessions` row was inserted `2026-08-14T12:29:54Z`, thirteen hours later, and a build baked before a row exists cannot contain it. Fixed by deployment `d580b90c` at `13:15:08Z`; `npm run smoke:prod` reports `1 session(s)`. **The lesson is the ordering** — migrations, then credentials, then *a build* — and the third step is the one that looks optional, because the database was already correct and the site still was not. |
| **⚠️ Verify the two configuration invariants at every promotion** | `process` | Neither lives in this repository, so nothing here goes red when one drifts, and **both were once correct and silently stopped being so**. **(1) Production's schema is not behind the repo** — it ran three migrations behind while `dev` advanced; ask the catalog per migration with the queries now in [`docs/reference/supabase.md`](./reference/supabase.md), and ⚠️ **not** `supabase_migrations.schema_migrations`, which lists `0001,0002` on a database holding all seven. **(2) Cloudflare deploys `main` only** — Workers Builds was set to deploy *every* branch, so `dev`'s `61030c4` (a docs commit) became the live site 108 s after its push; the non-`main` command is now `npx wrangler versions upload`. Both are on the release-gate checklist in CLAUDE.md. See [`docs/reference/deployment.md`](./reference/deployment.md) for the timeline. |
| ~~**`npm run smoke:prod` passes on a blank agenda**~~ | **done** | Its `/agenda/` sentinel was `/class="(sessions\|empty)"/` — the list *or* the empty state — so it went green on all 14 routes while the club's one session was off the site. Now `/<li class="session\b/`, with a `whenMissing` message that names the actual cause (a build baked before the row existed, or credentials that never reached `fetch-agenda.mjs`) rather than saying "the session list is missing" on a page that rendered its empty state perfectly, and a `detail` hook that prints the count. ⚠️ **The cheap half was the right half:** the "real" fix — querying the database from the smoke check — would put Supabase credentials into a check that currently needs none, to answer a question that "zero" already answers, since **zero is never correct for a club that meets weekly**. Proven both ways against the captured blank page and the live one: old sentinel passes on both, new one fails the blank. It still cannot know the count is *right*; that comparison stays on the release-gate checklist. |

⚠️ **`mogadorchess.ma` stays a separate, later option and blocks nothing.** It
needs a Moroccan registrar and can require paperwork; the subdomain needed
neither. If it ever lands it is the same three config touch points again plus a
redirect — cheap, and no reason for anything to wait on it. That includes
**custom SMTP**, which needs *a domain you control* rather than that specific
one.

## Accounts and privacy

| Item | Status | Note |
|---|---|---|
| ~~**31.9 KB of switched-off code is precached**~~ | **done** | Fixed in `scripts/build-sw.mjs`. ⚠️ **The measurement in the original entry was wrong and the fix corrected it: 29.9 KB across 12 files, not 31.9 across 13.** `child.js` looks like an auth chunk and is genuinely reachable — `progress.ts` → `progress-sync.ts` → `child.ts` is live on every board page — so a hand-written `globIgnores` list naming "the auth chunks" would have pulled two live modules out of the offline cache. The fix therefore asks the BUILD which assets no emitted page can reach, transitively, and excludes those; with accounts ON it finds nothing, which is the same code proving itself. Precache went 162 → 150 files, 5983 → 5953 KiB. Guarded twice: the build fails if an exclusion did not take effect, and `pwa.spec.ts` asserts it in both flag states — **after** asserting the chunks exist, because "no admin chunk in the manifest" passes perfectly on a build that has none. |
| ~~**⚠️ The release matrix goes red on this machine, and it is getting worse**~~ | **done** | Fixed in `scripts/test-release.mjs`: the matrix now runs **one project at a time at three workers** — **0 failures in 66.8m**, against v0.11.0's 4 in 43.9m and v0.11.1's 7 in 58.3m. ⚠️ **The cause was memory exhaustion, not a browser bug and not a test bug.** Playwright shares **one worker pool across every project**, so the default six workers meant six *mixed* browsers side by side: **80 processes, 6.68 GB, 2.08 GB of 15.8 GB free** — the point at which Firefox's software compositor cannot allocate. That is why it landed on a different spec every run and why every one passed serially. All three options in the original entry were settled with numbers: **A** (per-project, 3 workers) shipped; **C** (pooled, 3 workers) was green but *not cheaper* — 51.7m for only the three failing projects, so pooled-over-five lands above A, and it looked cheap because it did less; **B** (`fullyParallel: false` on firefox) was **rejected without a run**, because webkit and iphone-13 already carry that setting and were two of the three projects failing both gates. ⚠️ **Honest caveat, recorded in `docs/reference/testing.md`:** C's green run suggests the **worker cap**, not the per-project split, is the half that does the work — the split is kept for the per-project accounting it enables (a project that runs zero tests is now caught; the old "total divides by 5" check could not see that). ⚠️ **Not fixed by loosening timeouts** — tried on `play.spec.ts`, the failure count went UP. **The gate is now expected to be GREEN: a red matrix is a finding, not a known flake.** |
| ~~**Self-service account deletion**~~ | **done** | Migration 0007 `delete_own_account()` — no target parameter, `authenticated` only. Two steps on `/compte/`, the second a typed `SUPPRIMER`, and the confirmation names children, progress, games, points and attendance. **Nothing is retained** — no statistics, no archive, no anonymised copy — and a spec asserts that rather than the notice claiming it. Audited live on the test project: every table 1 → 0 in 453 ms, auth user gone, the club's session kept with `created_by` nulled. `/politique-confidentialite` changed in the same commit. |
| ~~**⚠️ `anon` holds `TRUNCATE` on every public table but `profiles`**~~ | **done** (production pending) | **Migration 0008.** Found auditing production's catalog on 2026-08-14, and true of the test project too. **No migration granted it** — a Supabase project ships `alter default privileges in schema public grant all on tables to anon, authenticated`, which fires on `create table` before a migration's own `grant select` line is reached, so the later grant narrowed nothing. `profiles` escaped only because 0001 happens to `revoke all … from anon, authenticated` first. ⚠️ **`TRUNCATE` is not filtered by RLS** — what was stopping it is that PostgREST exposes no verb reaching it, and *reachability is not authorisation*. Not exploitable with the anon key, so defence-in-depth rather than an incident. 0008 revokes on all seven, **gives `sessions` its `select` back** (⚠️ `fetch-agenda.mjs` bakes the agenda with the anon key — a bare `revoke all` there would empty `/agenda/` on every future build), and cancels the default-privilege entry so the next table cannot inherit it. Verified on the test project by exercise, not by reading ACLs: `anon` now appears exactly once, on `sessions`, holding `SELECT`; a throwaway table created as `postgres` grants `anon` nothing. ⚠️ **Applied to the TEST project only** — production needs it run by hand, like every migration here. |
| **`authenticated` also inherits `TRUNCATE` on every table** | `backlog` | Same mechanism as the `anon` entry above, deliberately left out of 0008 — that migration is about `anon`, and `authenticated` legitimately holds DML on most of these tables and is confined by RLS. The question is whether `TRUNCATE`, `REFERENCES` and `TRIGGER` should be part of that set; almost certainly not, and PostgREST reaches none of them. Wants its own migration and its own thinking about what a signed-in parent's role ought to be able to name, not a line appended to a migration about guests. |
| **⚠️ Production's migration ledger is five behind its schema** | `seàn` | `supabase_migrations.schema_migrations` records `0001,0002`; the schema demonstrably holds everything through 0007. 0003–0007 were applied by a path that did not write the ledger (the SQL editor, most likely). **Harmless to the running site, dangerous to tooling:** a future `supabase db push` at production would attempt to **replay** five applied migrations, and 0005 carries `alter table … drop constraint exercise_progress_pkey` with no `if exists`. It would abort rather than corrupt — but it fails halfway through a promotion, on the table the whole child-profile model hangs off, with nothing pointing at the ledger. ⚠️ **The SQL is written and waiting in [`docs/ADMIN.md`](./ADMIN.md)** — it records history and executes nothing, `statements` left NULL deliberately. Run it in the Supabase SQL editor against production; ⚠️ **do not add a production path to `scripts/db-push.mjs`** to do it. **Note 0008 is now also unapplied there**, so both want the same sitting. |
| ~~**Parents are not guided through their first sign-in**~~ | **done** | `/bienvenue/`, v2-S5. A parent signed up, silently received one child profile **named from their email address**, and nothing anywhere suggested it could be renamed — so `nachiketas3d` would have gone on the attendance sheet. One skippable screen: name the student, add a sibling, done. ⚠️ **Shown once per ACCOUNT** (`profiles.onboarded_at`), not per device — in `localStorage` a parent would be re-asked the first time they opened the family tablet. ⚠️ **Skipping is a first-class outcome** and `onboarding.spec.ts` asserts the account is fully usable afterwards. ⚠️ **The placeholder is never pre-filled**, detected by exact match against the email local part rather than a guess about what names look like. |
| ~~**`/compte/` does not say who holds the account**~~ | **done** | An explainer block at the top, plus « Titulaire du compte » as the label on the email. ⚠️ **The copy names the STRUCTURE, never the relationship**: an account with one child is the same object for a parent and for an autonomous teenager, and the site cannot tell them apart (CF40). So the heading is « Les élèves de ce compte », never « Mes élèves », and the one-child sentence covers both readings in one clause. The teenager case is **named out loud** rather than left ambiguous. |
| ~~**Anyone can create junk accounts and nobody can see them**~~ | **done** (partly config) | `/admin/comptes/` — sign-ups newest first, with confirmation state and activity, and a two-step removal requiring a typed reason. **Admin only**, not prof. Plus a honeypot on `/connexion/`. ⚠️ **The honeypot is noise reduction, not security** — the anon key is public and the endpoint is reachable with `curl`. ⚠️ **A CAPTCHA is not a drop-in**: it is a third-party script on a public page, which CF9 forbids outright, so adopting one is a policy decision rather than a wiring task. **The server-side half is a dashboard setting and is NOT done — see below.** |
| **Supabase OTP rate limits are at their defaults** | `seàn` | The server-side half of sign-up hygiene, and the only half a determined human cannot walk around. Supabase → Authentication → Rate Limits: per-address and per-IP caps on the OTP endpoint. It is a dashboard setting, so nothing in this repository can assert it — which is exactly why it is written down here rather than assumed done. Pair it with a look at Auth → Providers → Email → "Confirm email" while you are there. |
| **A fuller deletion audit is a privacy decision, not a schema one** | `backlog` | `account_deletions` holds `deleted_at`, `deleted_by`, `reason` and **no reference at all** to the removed account — a deliberate reading of CF51, whose "no statistics, no archive, no anonymised copy" was written for the self-service button and binds a volunteer pressing the admin one just as hard. If Seàn ever wants to know *which* account was removed, that is a real tradeoff to decide out loud: it would mean retaining something about a person after they were erased. ⚠️ A spec asserts the column list, so adding `target_id` "because it would be useful" fails a test rather than quietly changing what erasure means. |
| **⚠️ The preview server died mid-project twice during the v0.13.0 gate** | `bug` | Both times as **371 × `page.goto: Could not connect to server`** on a single project, against ordinary content routes (`/parametres/`, `/pieges/legal/`, `/`) — so the site simply stopped being served on 4321 while tests kept running. ⚠️ **Not a browser crash**: the documented failure mode is a starved *compositor* and reads as bare timeouts or `browserContext.close` protocol errors; this is the *server* going away, which is a different fault with the same smell. The first occurrence had an identified cause (another project held 4321, so `reuseExistingServer` and the preview's silent move to 4322 fought each other); the second had **both ports free at pre-flight and no leftovers afterwards**, and is unexplained. Suspect memory: the machine had **3.8 GB free of 15.8 GB at idle**, and `webServer.command` is `npm run build && npm run preview`, so the matrix runs **five full builds**, one per project, each alongside three browsers. Worth measuring before assuming — and worth asking whether the projects need to rebuild at all when only the browser changes. ⚠️ **Do not "fix" it by raising timeouts**: that was tried on `play.spec.ts` and made things worse. |
| ~~**`feel.spec.ts` "a button translates and tightens its shadow" is flaky on WebKit**~~ | **done** | It sampled the computed transform **once**, after a fixed `RESPONSE_MS * 2` wait, and on WebKit intermittently read `matrix(1, 0, 0, 1, 0, 0)` — the identity, meaning `:active` had never been applied rather than being mid-travel. Failed first-attempt at `--workers=1` and passed on retry, and in the v0.13.0 matrix it **exhausted WebKit's single retry and turned the gate red** on a test whose subject (the home CTA) that release never touched. Fixed by hovering through Playwright's own actionability rather than aiming at a once-read bounding box, and by **polling while held** instead of sampling after a fixed wait. The assertion keeps its teeth — the transform must become a real translate — only the deadline moved. 3/3 green at `--retries=0` where it previously failed first-attempt. |
| **Three more axe checks sample before the page has settled** | `backlog` | The v0.13.0 gate cost three full matrix runs, and **two of the three blockers were the same mistake**: a test sampling a state on a signal that does not mean what it looks like it means. `mouse.down()` does not mean `:active` has applied; `toBeVisible()` does not mean a transition has finished. Both are fixed, and `settleAnimations()` now sits beside `settleReveals()` in `tests/e2e/helpers/reveal.ts`. ⚠️ **`progression.spec.ts:428` has the identical shape and stayed flaky in the green run** — it awaits `[data-score-achievement][data-earned]` being visible and runs axe immediately, and went flaky on firefox at `/progres/ fr — terminal dark`. It passed on retry so the gate is green, but it is the next one to bite. The fix is one line (`await settleAnimations(page)`); it is not done here only because the tree had already been gated. **Sweep the other axe checks for the same pattern at the same time** — the rule is that axe measures a settled page, and only `settleReveals()` was ever written down. |
| **⚠️ `family.spec.ts:267` waits for the section, not for the first paint** | `open` | Went flaky on the v0.16.0 accounts-ON matrix with a signature that is **not** a timeout: `toHaveText` got `["Sara"]` against `["Sara","Yassine"]`, the locator resolving to **1 element 13 times running** — a roster painted once and never repainted. Investigated properly rather than waved through, and it **did not reproduce: 6/6 clean** at `--project=firefox --workers=1`. ⚠️ **Two mechanisms were hypothesised and both REFUTED, so neither should be re-argued from scratch.** (1) *The add submits before `load()` sets `accountId`, inserting `account_id: ''` into a `uuid` column*: the form **is** server-rendered, enabled, and live before the first load, but an end-to-end probe with PostgREST stalled 6s passed cleanly — `accountId` is assigned immediately after `getUser()`, which reads the session from `localStorage` without a network call, so the window is far narrower than it looks. (2) *A generation strand via `resolveChild()`'s picker branch firing `CHILD_EVENT` and invalidating the submit's own load*: refuted by `if (active) return active` at the top of `resolveChild`, which short-circuits every call after the first adoption — the branch that dispatches is never reached on this path. ⚠️ **What IS real and unrefuted:** `load()` has early returns (`if (!user) return`) that **consume a generation without painting**, so a newer load that returns early strands the previous one's already-dropped result with **nothing to re-trigger a paint** — the exact observed signature. Not reproduced, so not confirmed. ⚠️ **The cheap half is certain and should be done regardless:** the sibling test at `family.spec.ts:80` waits `toHaveText(['Sara'])` before filling the form and does not go flaky; **:267 waits only for `[data-testid=family]` to be visible**, which is the server-rendered shell. That difference alone explains why :267 is the one that flakes. ⚠️ **Pre-existing since v0.12.0 and live on `main`** — not a v0.16.0 regression, which is why the release was not held for it. ⚠️ **The original failure's trace was destroyed** by the repetition runs (Playwright clears `test-results/` per run); preserve it next time before re-running. |
| **Two-year inactive-account rule** | `backlog` | Stated as policy, enforced by nothing. Needs a scheduled job — most likely a Supabase cron, since this architecture has nowhere else to put one. |
| **Custom SMTP (Resend)** | `backlog` | Needs **a domain you control**, not specifically `mogadorchess.ma` — `mogadorchess.nachi3dlabs.com` satisfies it, so this is no longer blocked (see Deployment and domain). Supabase's built-in mailer is rate-limited, sends from an unfamiliar domain, and its template is untranslated — all three matter when the recipient is a parent being asked to click a sign-in link. Verify SPF/DKIM/DMARC when it lands. |

## Design direction

| Item | Status | Note |
|---|---|---|
| **Refonte esthétique majeure** — make it feel like a GAME | `in progress` | Direction written and approved: `docs/direction/mcc-direction-esthetique.md`. Sequenced E1 → E4; see below. |
| **E1 — motion vocabulary + action feedback** | **done** | Three families, the press, the correct-move pulse, the wrong-move reason, the two-beat solve, a second ambient layer. |
| **E2 — sound** | **done** | Six synthesised voices in `src/lib/sound.ts`, off by default, own key `mcc:sound:v1`, three volume steps, one-time invitation at the first solve. No audio files: 0 precache bytes, 0 requests, no licence question. ⚠️ Departs from the direction doc on one point — `prefers-reduced-motion` does **not** silence the site, only the unprompted offer. See CLAUDE.md → Sound. |
| **Is the sound palette right?** | `seàn` | The suite proves the plumbing and cannot hear a thing. Three questions only a person can answer, all in `docs/MANUAL-TESTS.md`: does a correct move feel satisfying; does a wrong move feel **corrective rather than punishing** (the one that matters — it teaches children); and is anything grating after twenty exercises. Every voice is four numbers in one file, so re-tuning is cheap. |
| **E3 — progression** | **done** | Ranks Pion → Cavalier → Fou → Tour → Dame at 0/20/70/150/220, a derived point ledger, session streaks and seven achievement kinds. **Session streaks only — never a daily streak** (now Critical Feature 34). Still wants v2-S3 to mean anything across devices. |
| **E3 — "a trap mastered" achievement** | `blocked` | Deferred out of E3 on purpose. A trap page is a *replayer* and records nothing, because stepping through a game someone else played is reading rather than competence. Shipping it today would mean awarding it for scrubbing a replay to the end — the "rank earned by clicking" the direction forbids. **Blocked on a trap carrying an exercise**, which is content work plus a schema field, not progression work. |
| **E3 — serve the score catalogue as one file** | `backlog` | The resolver inlines a ~3.4 KB catalogue and a ~5 KB script on ~62 of 86 pages (+744 KiB precache, uncompressed). Already trimmed three ways — terse script, no entry ids, shared toast CSS. The remaining win is serving the catalogue as one same-origin JSON and inlining it only on `/` and `/progres/`, where it is needed in the first paint. Costs a request on board pages; judged not worth the complexity yet. |
| **E5 — retro main menu on the home page** | **done** | Six entries, roving tabindex, "Reprendre" resolving to the furthest incomplete step. |
| **E6 — complete themes** (background + board + pieces) | **done** | Four themes, each with both palettes, four licence-checked piece sets and a sixth board preset. The constraints it shipped under still bind anything added later: every piece set needs its own attribution on `/mentions-legales/`, verified set by set; texture in CSS only, never images; and every theme clears `check-contrast.mjs` on all its pairs in both modes **at design time**, not at the end. |
| **E7 — thematic typography** | **done** | Heading face per theme; **the body family never changes** (Critical Feature 23). One theme loads one display font, never four. |
| **E8 — the shop** | `blocked` | ⚠️ **One purse per CHILD, and an order records the split when a purchase draws from several** — the parent/child model is decided and the shop must be built against it, never against a shared family wallet. Catalogue display needs nothing; **the points exchange cannot open before v2-S3**. Points live in `localStorage` while accounts are off, so changing phone loses them — and that is a lost *reward*, not lost progress. Once accounts exist the balance must be computed in the database from exercises actually solved, never accepted from the client. **Points are never sold.** |
| **E4 — vocabulary and atmosphere** | `backlog` | Evocative names on **page titles only**; nav labels stay functional (Cours, Exercices, Jouer). ⚠️ Now constrained by E5 as well: the home menu takes its labels from the same `nav.*` keys, so renaming a nav label renames a menu entry too. May be absorbed by E5 + E7 — see the addendum. |

### Refonte esthétique majeure — a direction session, not a patch

The site should read as a **game first and a learning site second**: more motion
around buttons, boards and backgrounds — playful, alive, arcade-adjacent.

What it must NOT cost:

- **The "old chess club" identity.** Wood, baize, brass, yellowing score sheets.
  Arcade energy inside that world, not instead of it.
- **The AA contrast guarantees.** `check-contrast.mjs` runs first in the build,
  both palettes, every board preset. A livelier palette still has to clear it.
- **`prefers-reduced-motion`.** Every added motion needs its off switch, and
  "off" means off, not faster.
- **No GSAP.** Its licence is not OSI and conflicts with this project's
  GPL-3.0-or-later (see the note in CLAUDE.md). CSS and small vanilla JS have
  carried the ambient motion so far at ~1.3 KB; a new library must clear the
  same licence bar before it is even evaluated on merit.

~~⚠️ **This wants a written direction and Seàn's sign-off before any
implementation.**~~ RESOLVED — the direction is written, approved, and lives at
`docs/direction/mcc-direction-esthetique.md`. Its guiding line: *un site donne
l'impression d'un jeu quand il **répond**, pas quand il est déguisé.* Order of
work is **feel (E1) → sound (E2) → progression (E3) → dressing (E4)**, and the
board stays sober throughout.

---

## Teaching and content

| Item | Status | Note |
|---|---|---|
| ~~**Lessons built around a YouTube video**~~ | **done** | The click-to-load facade shipped: `VideoFacade.astro` renders a self-hosted still, a play button and the title, and contacts nothing until the reader presses it — then an iframe on `youtube-nocookie.com`. Mounted on `/pieges/<slug>/` and `/cours/<slug>/`, **below** the page's primary content so the board is never displaced. `tests/e2e/video.spec.ts` asserts zero third-party requests before the click and was watched to go red on a hot-linked poster. `/mentions-legales/#video` states what a click sends. |
| **Michael's first video** | `waiting` | ⚠️ **No published trap or course carries a `youtube` id.** The facade is built, tested and merged; the exercised path is a FIXTURE (`src/content/traps/fixture-video-facade.json`, routable only under `PUBLIC_FIXTURES=true`, listed nowhere), so nothing placeholder reaches a reader. **When the first real video lands:** add the id to the real entry → `node scripts/fetch-video-posters.mjs` → commit `public/video/` → update `video.spec.ts`'s *"real published content carries no video"* test **deliberately**, in the same commit. ⚠️ **The id, the poster run and the release go together** — an id merged without a deploy is a poster nobody has. **No promotion to `main` until then.** |
| ~~**Beginner tutorial** (`/apprendre-les-bases/`)~~ | **done** | 13 steps, both locales, shipped this session. |
| ~~**Course detail pages**~~ | **done** | Per-locale Markdown pairs shipped with course 1. A `lessons` collection keyed by `course` + `slug` + `lang`; routes are `/cours/<course>/<lesson>/`. |
| ~~**Course 3 — "Les motifs tactiques"**~~ | **done** | Seven lessons, both locales, shipped from content batch 3. Four of its eight positions had to be rebuilt first — see CHANGELOG. |
| ~~**Batch 5 — 24 exercises**~~ | **done** | `/exercices/` is 27 entries with filtering by level and by theme. Every position was built and verified against chess.js by a workbench (`docs/content-batches/batch-5-exercices.md` → CHANGELOG for what it caught). `claims[]` and `forcedReplies` now exist on the collection and are proved by the build. ⚠️ **The filters are static routes, not `?niveau=`** — static output leaves no server to read a query string; see the CHANGELOG for the rejected alternative. |
| **Courses 4+** | `backlog` | Courses 1, 2 and 3 are written. The structure is proven; the remaining work is authoring. |
| ~~**Author FENs need a claim-level check**~~ | **done** | `claims[]` on `position` and `exercise` boards, asserted by `check-content.mjs`: `pin`, `fork`, `discovery`, `line`, plus `manual` with a required note. Each assertion was verified to fail on the real broken position before being trusted. See CLAUDE.md → "A legal position is not a correct one". |
| **Retrofit `claims[]` onto courses 1 and 2** | `backlog` | 17 boards predate the check and sit on the manual review queue as *"no claim declared"*. Not urgent — they have been read by humans and shipped — but the queue is only useful while it shrinks. Do it opportunistically, when touching a lesson anyway. Only `position` and `exercise` boards are covered; **trap replayers and `tutoriel` steps are not** and would be a separate decision. |
| **A trap cannot show its own refutation** | `open` | Every trap teaches "how not to fall for this", but a trap has **one** `pgn` and the replayer walks a linear ply list, so the refutation can only be described in prose and asserted in `claims[]` — never played out on the board, which is what batch 4's brief asked for. PGN variations (RAV) would need parser and replayer support. The cheap version is an optional `refutation: { pgn, moveComments }` plus a second `ReplayBoard` on `TrapPage`; the batch-4 brief pinned the schema, so it was not done. |
| **Trap prose cannot carry a link** | `open` | `summary` and `moveComments[].text` both render as plain text (`<p>{summary}</p>`, `{comment.text}`), so batch 4's cross-links to the course-3 motifs are **named in prose rather than hyperlinked** — "relis la leçon « Le clouage »" instead of a link. Lessons link to traps fine (Markdown); only the reverse direction is stuck. An optional `related[]` of slugs rendered as links under the summary would fix it. |
| **Claim kinds a forcing search would unlock** | `backlog` | `skewer` and "wins the piece" need to prove something about *every legal reply*, which is a small search rather than a property of a position — deliberately left out so the checker cannot appear to cover what it does not. Three course-3 boards are on the manual queue for exactly this. Stockfish is already in the repo; if this is ever wanted, it is a lab-style script, not part of the build. |
| **MDX for lesson bodies** | `backlog` | Boards are currently placed with a `<!--board-->` marker split out of the rendered HTML. It works and costs nothing, but MDX would allow real components inline. Only worth it when a lesson needs something a marker cannot express. |
| **Engine-backed validator for `onlyMove: false`** | `backlog` | The remaining half of the exercise-validation rule. Stockfish is here now; this is what finally lets a winning alternative be accepted instead of "not the line we had in mind". |
| **Printable handouts from the PGN** | `backlog` | — |

## Board and interaction

| Item | Status | Note |
|---|---|---|
| **Promotion picker on the pointer path** | `backlog` | Currently backwards: the typed path honours `e8=D`, the pointer path auto-queens. A keyboard user therefore has strictly *more* control than a mouse user, which is the wrong way round. No shipped exercise promotes, and a queen is right ~99% of the time against the engine — so it is real but not urgent. |
| **Pass-and-play** (two players, one device) | `backlog` | Not free from `PlayView`: no engine, no thinking state, a board that flips or does not, and a second result vocabulary ("White wins" rather than "you win"). A small separate mode, not a flag. |
| **"Nocturne" board preset** | `resolved` | Superseded by E6. `phosphore` is the sixth preset, and every theme now names its own board — so "too bright in a dark room" is answered by choosing Terminal or Marbre rather than by another preset. Reopen only if a reader wants a dark board *inside* a light theme. |
| **AGPL piece sets** (`pixel`, `letter`, `pirouetti`) | `conditional` | Free software, and NOT a licence conflict — but AGPLv3 §13 adds a network-use obligation the repo does not currently carry, so adopting one changes the licence statement on `/mentions-legales/`. `pixel` would suit Terminal well. Needs Seàn's decision, not a session's. |
| **Old-style figures in body text** | `blocked` | Declared in `typography.css` and inert: Inter ships no `onum`. Only unblocked by changing the body face, which the E7 safety rule forbids doing per-theme. A spec reports whether it ever starts working. |
| **The board BLOCK overflows a phone screen** | `open` | **Measured in M3, not fixed.** At 390×844 (791px usable) the exercise block is **833px** and the trap replayer **895px**; at 360×640 (587px usable) they are 828px and 865px. The *board* is fine — 335px — so this is not a sizing bug: the other ~500px is the control stack (tag, move field, buttons, hint, verdict). Compressing it is a design decision about what an exercise shows at once, which is why M3 recorded it rather than tweaking CSS. Re-measure with `scripts/measure-board.mjs` (scratchpad, M3 session) or rebuild it — it walks `.mcc-board-block` at both viewports. |
| ~~**A killed matrix leaves orphaned Playwright browsers**~~ | **done** | `orphanedBrowsers()` in `scripts/demo.mjs`, inside the same `sweep()` as the preview cleanup, so it runs on startup and on Ctrl+C. Matches on `ExecutablePath` under a Playwright browsers root (`PLAYWRIGHT_BROWSERS_PATH` first — the documented default is wrong on this machine), **never on the process name**; kills tree roots only, with `taskkill /T`, and **only when orphaned** so a run in progress is never touched. Verified live: 25 outside-cache browsers spared, 3 spared while their launcher lived, 1 orphan reported and 4 processes gone. See CLAUDE.md → the sweep. |
| **The M1 mobile header wraps at 360px** | `open` | 61px at 390px, **97px at 360px** — the "one line" header becomes two, eating 36px of an already short screen. Found in M3. |
| **The desktop header wraps between 768px and 1023px** | `open` | **77px → 129px**, introduced by adding the fifth nav entry (`/progres/`) — it adds 72px of nav width and pushes `header-inner` past one line in that band. Verified against `dev` that the header already wrapped at 768px without it, so this widens an existing behaviour rather than creating one, and 1024px+ is untouched. **Not fixable by trimming the gap** — the four `nav-root` gaps hold 16px at 0.25rem against the 72px needed. The real options are a narrower brand (hiding the club name below 1024px, reclaiming ~150px) or accepting two rows; the first is a design decision, not a session's. Accepted for now and measured rather than papered over. |

## M3 — app density, unfinished items

M3 delivered the card consolidation and three-state progress. These were in the
brief and are **not** done:

| Item | Status | Note |
|---|---|---|
| **"Reprendre" card on `/cours` and `/exercices`** | `open` | Decided with Seàn during M3: extract the E5 resolver from `HomePage.astro` into one `ResumeCard.astro` (props: locale, journey, variant) with four call sites — Home, Dashboard, `/cours`, `/exercices`, `/progres`. It must stay `is:inline` (it runs before first paint; measured CLS 0.000) and `/exercices` needs its own journey, since standalone exercises are not in the E5 journey at all. |
| **Board fit and prev/next clearance** | `open` | See the two rows above. |
| **`/progres` substance** | `open` | Currently three bars. The brief wants exercises solved **by level and by theme**, lessons completed, tutorial progress, and what remains — all from data that exists today. Rank and points stay "bientôt" until E3, as the dashboard already does. |

## Beyond v2

| Item | Status | Note |
|---|---|---|
| **Online play** — room codes + Durable Objects | `v3` | `MoveProvider` in `src/lib/chess/opponent.ts` is already the seam: a new implementation plus a lobby, not a board rewrite. |
| **Spectate a room** | `v3` | Follows online play. |
| **Decorative 3D hero** | `v3` | Would have to clear the same bar the ambient motion did: no new dependency with a licence that conflicts with GPL-3.0, and Lighthouse ≥ 90 on home mobile. |
| ~~**CLAUDE.md is past the size guard's WARN line**~~ | **done** | Split at v0.16.0: **127,968 → 115,276 characters (85% → 77%)**. Five detail blocks moved VERBATIM into the reference set — the environment-symptom table and the four board-driving gates to `testing.md`, the migration checklist and the account surfaces to `supabase.md`, the two configuration invariants to `deployment.md` — each under a **Read when** line. The binding rules stayed in CLAUDE.md. `check-split.mjs` proves it: **222 lines moved, 1,251 stayed, nothing lost and nothing newly declared obsolete.** |

## Waiting on Seàn

| Item | Status | Note |
|---|---|---|
| **`mogadorchess.ma` domain** | `seàn` | ⚠️ **No longer blocks anything.** `mogadorchess.nachi3dlabs.com` gives the site a real address and unblocks custom SMTP with no registrar step (see Deployment and domain). `.ma` remains a separate, later option — a nicer name for a Moroccan club — and needs a Moroccan registrar and possibly paperwork. Decide it on its own merits, not under pressure. |
| **Club Instagram handle** | `seàn` | Does the club post through the association's account or its own? `site.socials` has the entry, unpublished. |
| **Brand mark** | `seàn` | The current one is an explicit placeholder — a board in a brass frame. |
| **Dar Souiri street line** | `seàn` | Which exact address may be published. |
| **Arabic / Darija locale** | `seàn` | A real question in Essaouira. The i18n layer supports a third locale structurally; RTL would need design work. |
| **FR pedagogy review of the tutorial** | `seàn` | Written this session — see the note in CHANGELOG. The chess is machine-verified; the *teaching* is not. |
| **Chess accuracy + FR pedagogy review of the six traps (batch 4)** | `seàn` | Every line is machine-verified legal and every declared mechanism is asserted on each build; the **judgements** are not, and they are the part that matters here. Four to look at. **(1)** The QGD trap was renamed from the brief's "piège de Lasker" to **le piège de l'éléphant** — the Lasker Trap is the Albin under-promotion line, a different opening. **(2)** `blackburne-shilling` is shipped as **unsound and says so in its own summary**; confirm that framing is the one you want in front of a twelve-year-old. **(3)** `fegatello` states that modern theory holds Black survives with best play — a verdict on a whole defence, not something any check proves. **(4)** ECO codes (C23, C57, D51, C50, C71, B17) are best-effort and unverified against a reference. |
| **FR pedagogy review of course 3 (les motifs tactiques)** | `seàn` | Same rule: every position and every line is now machine-verified, the *teaching* is not. Two specific things to look at. **(1) Lesson 5, la déviation** — `Ra8+` forces mate in two, so the "try it yourself" has a second, stronger right answer and `onlyMove` is `false`. The position cannot be fixed without removing the weak back rank the lesson is about, so the options are: accept it, replace the exercise, or let the engine-backed validator sort it out later. **(2) Lesson 2's added paragraph** — a short passage naming the d7 pawn was written by Claude (not in the brief) after the original position turned out to contain no pin at all; it is the most common beginner misconception about the Ruy Lopez and worth keeping, but it is unreviewed copy. |
| **Language switcher fails WCAG 2.5.3 (Label in Name)** | `bug` | PRE-EXISTING, found by Lighthouse during M1/M2 and not introduced by it. The switcher shows "English" but its accessible name is "Changer de langue", so the visible text is not in the name — voice control ("click English") cannot reach it. Zero-weight in Lighthouse's score, which is why it never surfaced. One-line fix in `LangSwitcher.astro`, but it touches a component with its own specs, so it was left rather than widened into an unrelated session. |
| **M3 — density pass on the inner pages** | **done** | Shipped across v0.6.0's M3 and v0.7.0's M3 (suite): one card definition, three-state progress, the compacted exercise controls, the shared resume resolver and `/progres` substance. |
| **⚠️ The correct-move pulse is dropped under fan-out load on WebKit** | `bug` | **ANNOTATED `fixme` ON `webkit` AND `iphone-13` ONLY (v0.7.0+).** It still runs with full teeth on chromium, firefox and pixel-5, which exercise the same code path — so a pulse that genuinely stops being drawn still fails the gate. **The feature works**: the test passes on both WebKit projects in isolation (`--workers=1`). ⚠️ **It is NOT a sampling artefact, and two sessions must not be spent proving that again.** Four independent samplers (rAF, `setInterval`, an observer on `cg-board`, an observer on the `.cg-wrap` host) recorded **35 mutation records and zero sightings**, including a `record.oldValue` check; a `data-pulse` probe showed ExerciseView's state committing and clearing normally (`'' → a8 → ''`). The break is therefore **below Preact and above the DOM**. **RULED OUT:** (a) the observer watching a `cg-board` Chessground had replaced — an identity tag proved the node was never replaced; (b) the clear-timer overtaking the apply in the move handler — fixing that did not resolve it. **CONFIRMED BUT INSUFFICIENT:** Chessground's `debounceRedraw` is rAF-scheduled *and* coalescing (`if (redrawing) return`), so a starved frame drops the intermediate state; the rAF gate added to `ExerciseView` moved `iphone-13` from hard failure to flaky but left `webkit` failing. **OPEN QUESTION, and where to start:** does `BoardSurface`'s update effect ever observe a non-empty `pulseSquare` under load, or does Preact's deferred effect flush coalesce the two values so `api.set()` is never called with the pulse at all? A probe for exactly this (`data-pulse-effect-saw` set inside the effect) was written and not run — the machine needed restarting. Answering it decides between a product fix and accepting the drop. |
| **Does Souiri feel like Essaouira?** | `seàn` | The identity theme, and the only part of E6 no machine can judge. `docs/MANUAL-TESTS.md` § Q3 has the questions; the last one is "show it to someone from Essaouira". |
| **Is Terminal readable or a gimmick?** | `seàn` | § Q4. The test is reading a whole lesson in it, on a phone, without switching away. If it fails, it is softened or dropped — it is the one theme that exists for fun rather than for legibility. |
